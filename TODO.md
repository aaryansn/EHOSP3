# TODO - Git initialization + first push

## Steps
1. Append `# EHOSP2` header to `README.md`.
2. Initialize git repository (if needed).
3. Stage `README.md`.
4. Commit as `first commit`.
5. Rename branch to `main`.
6. Add remote origin.
7. Force push to `main` (overwrite remote).

